#include<iostream>
using namespace std;
int main()
{
    // int arr[5] = {5,4,3,2,1}; // array declaration

    // for(int i =0; i<5; i++)
    // cout<<arr[i]<<endl;


    // int size;
    // cin>>size;

    // int arr[1000];
    // for(int i=0; i<size; i++)
    // cin>>arr[i];

    // for(int i =0; i<size; i++)
    // cout<<arr[i]<<endl;


/* The code is declaring an integer array `arr` with 5 elements and initializing it with the values 1,
2, 3, 4, and 5. */
    // int arr[5] = {1,2,3,4,5};


/* The line `cout<<sizeof(arr)/sizeof(arr[0])<<endl;` is calculating the size of the array `arr` by
dividing the total size of the array (`sizeof(arr)`) by the size of a single element in the array
(`sizeof(arr[0])`). */

    // cout<<sizeof(arr)/sizeof(arr[0])<<endl;

    return 0;
}